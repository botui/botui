{
  "private": true,
  "name": "botui-packages",
  "workspaces": [
    "docs",
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "test": "turbo run test",
    "link": "turbo run link",
    "build": "turbo run build",
    "unlink": "turbo run unlink",
    "danger-deploy": "turbo run danger-deploy",
    "clean": "rimraf './packages/*/{dist,.parcel-cache,node_modules}' './ui-tests/{dist,.parcel-cache,node_modules}'"
  },
  "devDependencies": {
    "rimraf": "^3.0.2",
    "turbo": "^1.5.3"
  }
}
