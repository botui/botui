{
  "private": true,
  "name": "botui-packages",
  "workspaces": [
    "docs",
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "danger-deploy": "turbo run danger-deploy"
  },
  "devDependencies": {
    "turbo": "^1.4.7"
  }
}
