{
  "private": true,
  "name": "botui-packages",
  "workspaces": [
    "docs",
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "test": "turbo run test",
    "link": "turbo run link",
    "build": "turbo run build",
    "unlink": "turbo run unlink",
    "danger-deploy": "turbo run danger-deploy"
  },
  "devDependencies": {
    "turbo": "^1.4.7"
  }
}
